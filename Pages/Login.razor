@page "/login"
@inject NavigationManager NavManager

<div class="text-center mt-5">
    <h2>Welcome Back 👋</h2>
    <p>Please log in using your GitHub account to continue.</p>
    <button class="btn btn-dark btn-lg mt-3" @onclick="LoginWithGitHub">
        <i class="bi bi-github me-2"></i> Sign in with GitHub
    </button>
</div>

@code {
    private void LoginWithGitHub()
    {
        NavManager.NavigateTo("https://ecommerceapiwebapp-daege4dme6bqbsak.uksouth-01.azurewebsites.net/auth/login", forceLoad: true);
    }
}